# Populador

Massa de Dados: Carga inicial de pelo menos 500 registros verossímeis para testes de volume

# EcoCiente — Carga inicial de dados (seed)

Gera INSERTs PostgreSQL verossímeis para todas as tabelas do schema,
respeitando todas as FKs, CHECKs e constraints UNIQUE.

Total de registros: ~580+ linhas de dados reais simulados.
